services:
  postgres:
    image: postgres:16
    container_name: gizmo-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gizmo4}
      POSTGRES_USER: ${POSTGRES_USER:-gizmo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./config/db:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 20
    ports:
      - "55432:5432"

  gizmo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gizmo-app
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /gizmo
      SPRING_PROFILES_ACTIVE: local
      LOG_DIR: /opt/app/logs
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-gizmo4}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-gizmo}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD-password}
      DB_SCHEMA: ${DB_SCHEMA:-public}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${SERVER_PORT:-8080}:8080"
    restart: unless-stopped

volumes:
  pgdata:
